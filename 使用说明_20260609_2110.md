# MinerU 文档解析系统 — 使用说明

> 文档生成时间：2026-06-09 21:10

---

## 一、前置条件

### 1. 激活虚拟环境

```powershell
.venv\Scripts\activate
```

### 2. 首次使用前下载模型（如未下载过）

```powershell
python scripts/download_models.py
```

模型约 2GB，从 ModelScope 下载，首次运行可能需要几分钟。

---

## 二、批量解析

### 基本用法

将待处理文件放入 `data/input/` 目录，然后运行：

```powershell
# 交互式：提示输入批次摘要
python scripts/batch_parse.py

# 命令行指定批次摘要
python scripts/batch_parse.py --summary "差旅报销"
python scripts/batch_parse.py -s "合同扫描"
```

### 文件命名规则

文件支持 `NNN_` 序号前缀（3 位数字），用于排序：

| 文件名 | 说明 |
|--------|------|
| `001_发票.pdf` | 序号 1 |
| `002_合同.jpg` | 序号 2 |
| `报告.docx` | 无序号 → 自动分配下一个序号 |

### 支持的文件格式

- **PDF** — 扫描版自动 OCR，文本版直接提取
- **图片** — jpg, png, bmp, gif, jp2, webp, tiff
- **Office** — docx, xlsx, pptx
- **XML** — 全电发票，自动转 Markdown 表格

---

## 三、输出结构

```
data/已识别/
└── {批次摘要}_YYYYMMDD_HHMMSS/
    ├── 源文件/          # 原始文件（带序号前缀）
    └── 识别结果/        # Markdown 文件（每个文件一个 .md）
```

每个 Markdown 文件自动包含：
- `## 摘要` — 提取的关键信息（日期、金额等）
- 正文内容 — 标题、表格、文本

---

## 四、示例流程

```powershell
# 1. 放入文件
Copy-Item "C:\扫描\*.pdf" -Destination "data/input/"

# 2. 运行解析
python scripts/batch_parse.py -s "10月差旅"

# 3. 查看结果
Get-ChildItem "data/已识别/10月差旅_*/识别结果/"
```

---

## 五、注意事项

- 解析完成后，输入文件会**移动**到输出目录的 `源文件/` 下
- 单个文件失败不影响其他文件，错误会记录在日志中
- 目录中的 `.zip` 文件会自动解压后再处理
