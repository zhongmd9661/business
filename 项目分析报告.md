# 项目分析报告 — 业务招待费智能审核系统

> 生成时间：2026-07-23

---

## 一、项目概览

| 项目 | 说明 |
|------|------|
| **名称** | 业务招待费智能审核系统（ocr-pipeline v0.1.0） |
| **定位** | 面向中国移动广东公司清远分公司的企业财务报销自动化审核系统 |
| **核心引擎** | MinerU OCR + 双引擎字段提取（正则 / LLM） |
| **技术栈** | Python 3.11+、FastAPI、Pydantic、SQLAlchemy、asyncio |
| **运行环境** | Windows 11 + NVIDIA CUDA |

---

## 二、核心架构（5 步流水线）

```
扫描文档 → OCR 识别 → 字段提取 → 规则审核 → 审核报告
```

### 源码模块（`src/`，共 36 个 Python 文件）

| 模块 | 文件数 | 职责 |
|------|--------|------|
| `batch_processing/` | 3 | 文件收集、ZIP 解压、序号排序、进度显示 |
| `document_parsing_pipeline/` | 3 | MinerU 引擎管理、PDF/图片/Office → Markdown |
| `expense_review_comprehensive/` | 9 | **核心**：字段提取、34+ 条规则检查、报告生成、Webhook 告警 |
| `output_visualization/` | 2 | 目录扁平化、摘要注入、Excel 报告 |
| `standards_query/` | 3 | 报销标准查询 |
| `web_service/` | 15 | FastAPI Web 服务：上传、审核、规则管理、标准设置、认证 |
| `application_form/` | 2 | 审批单 / 申请单 / 报账单表单生成 |

### 入口脚本（`scripts/`，共 14 个）

主入口 `batch_parse.py`，支持 `--once`（单次运行）和常驻监听两种模式。

---

## 三、审核规则体系

基于《中国移动广东公司清远分公司业务招待费管理办法（V9.0）》，**7 个规则类别、约 34 条规则**：

| 类别 | 规则数 | 要点 |
|------|--------|------|
| 单据完整性 | 7 | 报账单号一致性、支付凭证（2024-04 后强制）、支付流水（2025-09 后强制）、往来公函（2025-12 后外部强制） |
| 金额标准 | 5 | 外部招待矩阵（人员层级 × 招待类型）、内部招待（150/100 元）、工作餐（60 元） |
| 招待类型与陪同人数 | 3 | 类型-对象匹配、陪同比例限制 |
| 禁止性规定 | 6 | 高档场所 / 菜品、烟酒、批量购买、公款送礼、变相旅游 |
| 报销合规 | 6 | 事前审批、大额现金（>5000）、拆分报销 |
| 交叉稽核 | 4 | 企业经营状态、节日报备、重复招待 |
| 校对规则 | 6 | 金额 / 日期一致性、跨文档校验 |

严重级别：**高 → 中 → 低 → 提示**，规则支持时间线切换。

---

## 四、双引擎字段提取

- **规则提取器**（默认）：正则表达式 + HTML / Markdown 表格解析
- **LLM 提取器**：通过 Anthropic 兼容 API 调用本地大模型，规则从 `skill/` 动态加载

通过 `USE_LLM_EXTRACTOR` 环境变量切换。

---

## 五、Web 服务

FastAPI 应用，功能包括：
- 文件上传页面（9 个独立上传卡片）
- 智能审核页面（`audit.html`）
- 标准设置界面（可视化编辑报销标准，带密码保护）
- 提交记录管理
- JWT 认证（`python-jose` + `passlib`）
- SQLite 数据库（`aiosqlite`）

---

## 六、测试覆盖

26 个测试文件，覆盖批次处理、文档解析、费用审核（7 个子模块）、集成测试、Web 服务、校对规则和规则引擎。

---

## 七、项目成熟度评估

| 维度 | 评价 | 备注 |
|------|------|------|
| 文档 | ✅ | README 详尽，含架构图、目录结构、使用指南；OpenSpec 规范驱动 |
| 架构 | ✅ | 模块化清晰，流水线设计合理，asyncio 异步架构 |
| 测试 | ✅ | 覆盖核心模块，含 E2E 测试 |
| 配置 | ✅ | 环境变量驱动，双引擎可切换 |
| 规则管理 | ✅ | 动态加载，非硬编码，便于制度更新 |
| Web 界面 | ✅ | 文件上传、审核、标准设置、认证 |
| 生产就绪 | ⚠️ | 仍有临时调试文件（`temp_debug_*`），`.env` 仅 2 个变量 |
| 代码质量 | ⚠️ | 部分路径处理使用 Windows 风格（`\`），跨平台兼容性有限 |

---

## 八、总结

这是一个**功能完整的企业级财务审核系统**，从 OCR 识别到规则审核到报告生成的全链路自动化。设计亮点：双引擎字段提取、动态规则加载、时间线规则、批次聚合逻辑、四级严重级别。项目处于活跃开发阶段，已有 Web 界面但仍在迭代 UI 细节。
