# 测试材料使用说明

## 概述

本项目提供了一套完整的业务招待费审核测试材料，用于验证批次审核系统的各项规则检查功能。测试材料模拟了一笔真实的业务招待费用报销场景，包含从申请、审批、报销到支付的全流程单据。

## 测试材料清单

| 序号  | 文件名                              | 类型     | 用途               |
| --- | -------------------------------- | ------ | ---------------- |
| 1   | `1_【发票】26442000002098662676.pdf` | 增值税发票  | 验证发票金额、开票日期      |
| 2   | `2_【发票】26442000002098662676.xml` | 发票电子版  | 验证发票数据结构         |
| 3   | `3_业务招待审批单.jpg`                  | 审批单据   | 验证招待人数、陪同人数、招待类型 |
| 4   | `4_业务招待待申请单.pdf`                 | 申请单据   | 验证招待标准、人员级别      |
| 5   | `5_业务招待费报账单.pdf`                 | 报销单据   | 验证报销金额、费用明细      |
| 6   | `6_招待单位经营状态.PNG`                 | 经营状态查询 | 验证商户经营状态         |
| 7   | `7_支付凭证.jpg`                     | 支付凭证   | 验证支付金额、支付时间      |
| 8   | `8_支付证明.pdf`                     | 支付流水   | 验证交易一致性          |
| 9   | `9_活动函件.pdf`                     | 往来公函   | 验证活动日期           |

## 使用步骤

### 1. 准备测试材料

将测试材料文件夹复制到 `data/input/` 目录下：

```powershell
# 示例：复制测试材料到 input 目录
Copy-Item -Path "测试材料\案例标准测试材料" -Destination "data\input\" -Recurse
```

### 2. 运行批次审核

```powershell
# 激活虚拟环境
.venv\Scripts\activate

# 运行批次审核（处理完所有批次后退出）
python -m scripts.batch_parse --once
```

### 3. 查看审核结果

审核完成后，结果会输出到 `data/已识别/` 目录下，以批次名称和时间戳命名：

```
data/已识别/
└── 案例标准测试材料_20260617_HHMMSS/
    ├── 审核报告.md          # 审核报告（Markdown 格式）
    ├── 审核报告.txt          # 审核报告（纯文本格式）
    ├── 源文件/              # 原始输入文件
    └── 识别结果/            # OCR 识别结果和字段提取
        ├── 1_【发票】...md
        ├── 2_【发票】...md
        ├── 3_业务招待审批单.md
        ├── ...
        └── 字段提取结果.json
```

## 审核规则覆盖

测试材料覆盖了以下审核规则：

### 金额标准检查
- [x] 人均费用计算（实际金额 ÷ 总人数）
- [x] 招待标准对照（其他人员/其他公务标准 150 元）
- [x] 预算金额 vs 实际金额对比

### 校对规则检查
- [x] 金额一致性（审批单金额 vs 发票金额）
- [x] 日期一致性（招待日期 vs 发票日期 vs 支付日期）
- [x] 招待标准合规性（陪同人数限制）
- [x] 单位经营状态查询
- [x] 支付凭证一致性（商户名称比对）
- [x] 活动函件日期合理性

### 交叉稽核检查
- [x] 企业经营状态人工确认提示
- [x] 差旅期间招待人工确认提示

## 预期结果

使用标准测试材料运行审核，应产生以下结果：

| 指标 | 预期值 |
|------|--------|
| 文档数量 | 9 个 |
| 总金额 | 832.00 元（取发票实际金额） |
| 招待人数 | 5 人 |
| 陪同人数 | 2 人 |
| 人均费用 | 118.86 元 |
| 招待类型 | 其他公务招待 |
| 高风险问题 | 2 条（金额一致性、支付凭证一致性） |
| 提示问题 | 4 条（经营状态、差旅期间招待） |

## 注意事项

1. **测试材料是一次性消费**：运行审核后，input 目录下的材料会被清空。如需重复测试，请重新复制材料到 input 目录。

2. **OCR 识别时间**：首次运行需要启动 MinerU OCR 引擎，耗时约 1-3 分钟。后续运行会复用已识别的结果。

3. **LLM 字段提取**：系统会调用 LLM 进行字段提取，请确保网络环境和 API 密钥配置正确。

4. **聚合逻辑**：系统会智能聚合同一批次文档的金额和人数，避免重复计算：
   - 金额：取第一个可靠来源（发票 > 报账单 > 审批单 > 申请单）
   - 人数：取第一个非空值

## 故障排查

| 问题 | 可能原因 | 解决方案 |
|------|----------|----------|
| OCR 识别失败 | MinerU 服务未启动 | 检查网络连接，重启服务 |
| 字段提取为空 | LLM API 调用失败 | 检查 API 密钥和网络 |
| 审核报告异常 | 输入文件格式不支持 | 确保文件为 PDF/JPG/PNG/XML 格式 |
| 金额计算错误 | 多文档重复累加 | 确认 `_build_context` 聚合逻辑已修复 |

## 扩展测试

如需测试其他场景，可以修改测试材料中的关键字段：

| 测试场景    | 修改建议                       |
| ------- | -------------------------- |
| 超标招待    | 将发票金额改为 1200 元（人均 > 150 元） |
| 日期不一致   | 修改发票日期为招待日期后 10 天          |
| 人数超标    | 修改陪同人数为 5 人（超过标准 3 人）      |
| 商户名称不一致 | 修改支付凭证商户名称                 |
| 缺少单据    | 删除活动函件或支付凭证                |
